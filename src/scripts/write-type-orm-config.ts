// import { configService } from '../config/config.service';
// import fs = require('fs');
// const config =  as any;
// config['cli'] = {
//   migrationsDir: 'src/migration',
// };
// console.log(config);
// fs.writeFileSync('datasource.ts', JSON.stringify(config, null, 2));
